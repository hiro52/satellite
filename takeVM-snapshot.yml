---
- name: Create Snapshot VM by using vmrun command 
  hosts: all
  tasks: 
    - name: take snapshot
      ansible.windows.win_command:
        argv: 
        - vmrun.exe
        - snapshot F:\VM\RHEL9.1-1\RHEL9.1-1.vmx backup
